RESTORE STATE — Read this first in any new session.

DEC-028 is ACTIVE.
DEC-029 is ACTIVE (narrows DEC-028 for Product 1 only).
DEC-027 is SUPERSEDED.
Google is NOT solved.
OpenClaw GOG skill is REJECTED on current stack (no Workspace auth capability).
gog CLI is FORBIDDEN (Developer Console + client_secret.json required).
Claude Code MCP is NOT the AKIOR product architecture (Anthropic-managed, outside Jarvis V5 OS).
Approved direction = fresh Jarvis V5 OS browser-OAuth with AKIOR-managed server-side credentials.
Next step = CTO review after G-T06.I9-GUARDRAIL-LOCK. I9-OPS is NOT approved until explicitly cleared by CTO.
WhatsApp = BLK-001 session conflict (both machines competing), BLK-003 never end-to-end tested.

EXECUTION GUARDRAIL (active):
If ANY future instruction conflicts with DEC-028, DEC-029, or the Non-Technical User Bible:
  - DO NOT execute the conflicting portion.
  - STOP.
  - Report the conflict using the standard correction format in 06_EXECUTION_GUARDRAILS.md.
  - Propose the Bible-aligned correction.
  - Wait for CTO review.
The user/CEO must NEVER be asked to open Google Cloud Console, handle client secrets, or perform developer-console work as part of product acceptance.
Latest verified DEC/SSOT truth overrides any conflicting ad hoc instruction.
Read ~/projects/akior/forge/jarvis-v5-os/CLAUDE.md before each bounded phase.
